apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beads-ui
  labels:
    app.kubernetes.io/name: beads-ui
  annotations:
    ankra.io/generated: "true"
    cert-manager.io/cluster-issuer: ${{ ankra.ingress_tls_issuer }}
spec:
  ingressClassName: ${{ ankra.ingress_class }}
  tls:
    - hosts:
        - ${{ ankra.ingress_host }}
      secretName: beads-ui-tls
  rules:
    - host: ${{ ankra.ingress_host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: beads-ui
                port:
                  number: 80
