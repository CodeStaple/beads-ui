apiVersion: v1
kind: Service
metadata:
  name: beads-ui
  labels:
    app.kubernetes.io/name: beads-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: beads-ui
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
