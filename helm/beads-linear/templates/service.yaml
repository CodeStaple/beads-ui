apiVersion: v1
kind: Service
metadata:
  name: {{ include "beads-linear.fullname" . }}
  labels:
    {{- include "beads-linear.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beads-linear.selectorLabels" . | nindent 4 }}
