# Source: beads-linear/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: beads-linear
  namespace: beads
  labels:
    app.kubernetes.io/name: beads-linear
    app.kubernetes.io/instance: beads-linear
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.1.0"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: beads-linear
    app.kubernetes.io/instance: beads-linear
